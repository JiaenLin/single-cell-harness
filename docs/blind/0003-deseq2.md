# 0003 — DESeq2, by a cold agent, on the workstation: the first plugin written on the plan

**Date** 2026-09-12
**Tool** [DESeq2](https://bioconductor.org/packages/DESeq2) 1.52.0 — differential expression on
counts; R; no kernel in the family wraps it. Its figures are R-drawn, which is the whole point of
this record: harness ADR-0016 moved every figure call into the plugin's declaration
(`report.figures`, "the plan") and generates the R that draws it, and no cold agent has written a
plugin that way yet. cellchat was *migrated* onto the plan by hand-pasted worksheets; this is the
plan used from nothing.
**Agent** Claude Sonnet, cold: a clean-room copy of the harness at 9926017 and of scProfile at
9f4c05b under the session scratchpad (`git archive`, no git), the plugin-maker skill, a scratch
Python with anndata 0.10 / numpy / pandas / matplotlib / scipy / pyyaml as the host interpreter,
the workstation's R 4.6.1 with DESeq2 1.52.0 as the tool's interpreter, and nothing else. The
agent may read the tool's documentation. It may not edit anything outside the clean room; if the
maker is wrong it records that and works around it in the plugin. **Rooted in the clean room**
(0002's venue defect): it is started there and told nothing of any repository outside it.
**Venue** the workstation, build phase, plus whatever of the test phase the workstation can run:
the plugin's own selftest under the scratch Python and the real R. No cluster job, no cohort.
**Where the work lands** the clean room is not a home; the plugin lands in scProfile's
`tests/smoke/plugins/` as test material beside `enrichment.py`, as produced; the agent's report
and the measurements into this file.

## Measured before the agent starts

The maker's R probe, pointed at DESeq2 in the workstation's R (`sch dev convert inventory
--tool DESeq2 --rscript Rscript`): **7 functions** — `plotCounts`, `plotDispEsts`, `plotMA`,
`plotPCA`, `plotSparsity`, `show`, and `estimateDispersionsPriorVar`, the last reached only by
the body rule (its body calls one of R's plotting entry points; whether it draws is the agent's
to rule on). Five are the tool's documented plotting surface.

## Predictions, written before the agent starts

- **B1** `scprofile scaffold deseq2` writes the plugin skeleton and its companion, and the
  companion already carries the plan section and the interpreter (`.plan`, `.draw`,
  `.draw_all`) with an empty plan. `sch dev convert status` then reads the build owing every
  stage but `freshness`, which reads done for want of git in the clean room (0002 found this;
  it is correct by the stage's own rule and is not a debt).
- **B2** The inventory the agent takes is the 7 above; the agent rules on all 7, and
  `estimateDispersionsPriorVar` and `show` end up in `report.skips` with a reason from the
  closed vocabulary. The account worksheet speaks the plan form (a `"skips": {` block; a used
  function is an entry).
- **B3** **Zero hand-written draw sites.** Every figure the plugin draws in R is an entry in
  `report.figures` with `drawn_by`, `fn`, `axis`, `position`, `kind`, `args`/`expr`, `legend`,
  and the embedded R calls `.draw("<id>")` where a site would have stood. Measured by grepping
  the plugin for `npng(`/`ndev(`/`png(`/`pdf(` outside the generated companion: none. This is
  the cost line the ADR set, and it is the prediction that matters.
- **B4** Zero edits outside the clean room. `git status` clean in both real checkouts after.
- **B5** The build reaches 8 of 8 in the clean room by the agent alone, judgement included; the
  test phase stays `RUN?`; `memory_gb_*` are left undeclared, not invented.
- **B6** `scprofile validate deseq2` exits 0; `sch dev convert plan --rscript Rscript` prints
  0 that do not parse; every entry names a `kind` from the registry or `other`; the standard's
  self-check is untouched.
- **B7** The plugin's selftest passes on the workstation: it pseudobulks a `ctx.fixture` by
  sample, runs DESeq2 through the real R, and at least one panel is written through the
  companion's `.draw` — the first figure ever drawn from a plan an agent wrote.
- **B8** Maker defects the agent finds: **at most 3**, fewer than 0002's four, and at least
  one of them is about the plan (its first cold use): a worksheet that did not say what an
  entry lacks, a placeholder the interpreter could not evaluate, an entry key the validator and
  the maker name differently.
- **B9** The agent needs a person for method decisions (which design, which contrast, which
  pseudobulk) and for nothing the workflow should have absorbed; where it stalls on the
  workflow, it says so in its report and that is B8's evidence.

## Cost, filled in after

| | |
|---|---|
| maker defects found | |
| hand-written draw sites | |
| decisions that needed a person | |
| commands run | |
| job submissions | 0 (workstation) |
| edits outside the clean room | |
| agent | |

## Results

(written after the run)
